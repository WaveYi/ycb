<!--pages/couponDetail/index.wxml-->
<view>
  <!-- <block wx:if="{{is_buy == 1}}">
    <image src="{{user_code}}" class="user_code" mode="widthFix"></image>
  </block> -->
  <image class="coupon_img" lazy-load="true" bindlongpress="downloadImg" src="{{src}}" mode="widthFix"></image>
</view>